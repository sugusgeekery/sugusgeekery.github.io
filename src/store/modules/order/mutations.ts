import { State } from "./state";

export enum MutationTypes {
  // UpdateWeeklyReport = "UpdateWeeklyReport",
}

export default {
  // 更新周报数据
  // [MutationTypes.UpdateWeeklyReport](state: State, params: WeeklyReport) {
  //   const { weeklyReport } = state;
  //   const temp: WeeklyReport = weeklyReport;
  //   (function<T>(state: State, params: T, temp: T) {
  //     for (const key in params) {
  //       temp[key] = params[key];
  //     }
  //     state.weeklyReport = Object.assign(weeklyReport, temp);
  //   })(state, params, temp);
  // },
}